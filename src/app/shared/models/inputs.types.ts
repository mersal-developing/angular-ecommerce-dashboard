export enum InputFieldsTypes {
    TEXT = 'text',
    SLIDE = 'slideToggle',
    TEXTAREA = 'textarea',
    SELECT = 'select',
    FILE = 'file',
    RADIO = 'radio',
    NUMBER = 'number',
    DATE = 'date',
    CDK = 'cdk'
}